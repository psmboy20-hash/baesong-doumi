# 💻 다른 컴퓨터에 배송 도우미 설치하기

## ⚡ 제일 빠른 방법 — 명령 한 줄

1. Node.js 설치: https://nodejs.org/ko → 초록 LTS 버튼 → 설치
2. 키보드 **⊞윈도우키 + R** → `cmd` 입력 → 엔터 → 검은 창에 아래 한 줄 붙여넣고 엔터:

```
powershell -NoProfile -ExecutionPolicy Bypass -Command "irm https://raw.githubusercontent.com/psmboy20-hash/baesong-doumi/main/get.ps1 | iex"
```

프로그램 내려받기부터 바탕화면 아이콘, 부팅 자동시작, 실행까지 전부 자동으로 됩니다.
3. (기존 장부를 이어가려면) 옛 컴퓨터의 `C:\projects\ham\data` 폴더를 새 컴퓨터 같은 위치에 복사

---

## USB로 통째로 옮기는 방법 (위 방법이 어려울 때)

딱 **3단계**입니다. 10분이면 끝나요.

## 1단계 — Node.js 설치

새 컴퓨터에서 https://nodejs.org/ko 접속 → 초록색 **LTS** 버튼 → 내려받아 설치
(계속 "다음"만 누르면 됩니다)

## 2단계 — 폴더 복사

이 컴퓨터의 **`C:\projects\ham` 폴더 전체**를 USB(또는 구글드라이브)로 옮겨서
새 컴퓨터의 같은 위치 `C:\projects\ham` 에 붙여넣기

> ⚠️ `data` 폴더가 장부(주문/시딩/재고/카페24 연동 정보)예요. 폴더 전체를 통째로 복사하면 자동으로 포함됩니다.

## 3단계 — 설치 버튼 누르기

복사한 폴더 안의 **`설치.bat`** 를 두 번 클릭!

설치 버튼이 알아서 해주는 일:
- ✅ Node.js 설치 여부 확인 (없으면 설치 페이지를 열어줌)
- ✅ 바탕화면에 [배송 도우미] 아이콘 만들기
- ✅ 컴퓨터 켜면 자동 실행되게 등록
- ✅ 프로그램 바로 실행 + 화면 열기

**끝!**

---

## 참고

- 두 컴퓨터에서 **동시에** 쓰면 장부가 갈라집니다. 한 번에 한 컴퓨터에서만 쓰세요.
  (완전히 옮길 거면 옛 컴퓨터의 시작프로그램에서 "배송 도우미 자동시작"을 지우세요.)
- 카페24 연동은 폴더에 같이 담겨 가서 그대로 작동해요. 2주 이상 프로그램이 안 켜져 있었으면 ⚙️설정 → [카페24 연결하기]만 다시 누르면 됩니다.
- 구글시트 자동 기록(웹훅)도 설정째 복사되니 추가 작업이 없어요.

## 🔄 프로그램 업데이트는 저절로

- 노트북(원본 컴퓨터)에서 프로그램이 수정되면, **다른 PC들은 켜질 때 자동으로 새 버전을 받아 갈아끼웁니다.** 아무것도 안 해도 돼요.
- 노트북에서 업데이트를 내보내는 방법: 폴더 안의 **`배포.bat`** 더블클릭 (버전이 하나 올라가면서 배포됨)
- 업데이트 기록은 `update.log` 파일에서 볼 수 있어요. 장부(`data` 폴더)는 업데이트가 절대 건드리지 않습니다.
